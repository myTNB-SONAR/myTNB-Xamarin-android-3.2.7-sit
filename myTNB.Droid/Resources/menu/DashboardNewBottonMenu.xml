<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
  <item
      android:id="@+id/menu_dashboard"
      android:enabled="true"
      android:icon="@drawable/ic_menu_dashboard_selector"
      android:title="@string/menu_dashboard"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/menu_bill"
      android:enabled="true"
      android:icon="@drawable/ic_menu_bill_selector"
      android:title="@string/menu_bills"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/menu_promotion"
      android:enabled="true"
      android:icon="@drawable/ic_menu_promotions_selector"
      android:title="@string/menu_promotions"
      app:showAsAction="ifRoom" />

  <item
    android:id="@+id/menu_reward"
    android:enabled="true"
    android:icon="@drawable/ic_menu_reward_selector"
    android:title="@string/menu_reward"
    app:showAsAction="ifRoom" />

  <item
    android:id="@+id/menu_more"
    android:enabled="true"
    android:icon="@drawable/ic_menu_more_selector"
    android:title="@string/menu_more"
    app:showAsAction="ifRoom" />  
</menu>